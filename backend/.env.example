# Copy to .env and adjust for your local PostgreSQL instance.
# Format: postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/tracking

# Set to 1 to drop & recreate all tables and re-seed on startup (dev only)
RESET_DB=0
